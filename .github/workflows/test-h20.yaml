name: Test H20 Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, h20]
    steps:
      - name: System info
        run: |
          echo "Runner OK"
          nvidia-smi || echo "No GPU"
